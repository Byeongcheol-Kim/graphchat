# ============================================
# Branching AI 환경 설정
# ============================================
# 이 파일을 .env로 복사하고 값을 설정하세요
# cp .env.example .env
# ============================================

# 환경 설정 (development, test, production)
GRAPHCHAT_ENV=development

# ============================================
# LLM 제공자 설정 (최소 하나는 필수)
# ============================================

# Google Gemini (추천 - 무료 티어 제공)
# API 키 발급: https://aistudio.google.com/apikey
GOOGLE_API_KEY=your-google-api-key-here
GEMINI_MODEL=gemini-2.0-flash-exp

# ============================================
# 데이터베이스 설정
# ============================================

# FalkorDB (필수 - 그래프 데이터베이스)
# Docker 사용 시: localhost, 로컬 설치 시: 설치된 포트 확인
FALKORDB_HOST=localhost
FALKORDB_PORT=6379  # Docker Compose 사용 시 기본값
FALKORDB_GRAPH=branching_ai

# ============================================
# 애플리케이션 설정
# ============================================

# API 서버
API_HOST=0.0.0.0
API_PORT=8000
API_PREFIX=/api
CORS_ORIGINS=["http://localhost:3000","http://localhost:3001","http://localhost:3002"]

# 보안 (프로덕션에서는 반드시 변경!)
JWT_SECRET=dev-secret-key-change-in-production
JWT_ALGORITHM=HS256
JWT_EXPIRATION_HOURS=24

# 로깅
LOG_LEVEL=INFO
LOG_FORMAT=text  # json 또는 text

# 개발 모드
DEBUG=true  # true로 설정하면 LLM 입출력을 포함한 상세 로그 표시
